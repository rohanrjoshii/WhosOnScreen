{
  "name": "whos-on-screen",
  "version": "0.1.0",
  "private": true,
  "description": "Instantly identify actors on screen while watching movies and TV shows.",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
